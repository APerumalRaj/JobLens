# ===================================================
# JobLens Configuration
# ===================================================

spring.application.name=joblens-email-filter

# ===================================================
# Mail Configuration (SMTP)
# ===================================================

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=perumalraj@024@gmail.com
spring.mail.password=RajuA@2001

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ===================================================
# Gmail OAuth Configuration
# ===================================================

gmail.oauth.client-id=956600029837-4ds3lc3vibf1iplg1i0mir7qpoq8ia4d.apps.googleusercontent.com
gmail.oauth.client-secret=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

gmail.oauth.redirect-uri=http://localhost:8888/callback
gmail.oauth.token-file-path=tokens/
gmail.oauth.scopes=https://www.googleapis.com/auth/gmail.readonly

# ===================================================
# Job Filtering Configuration
# ===================================================

job.keywords=java,spring,spring boot,backend,rest,oracle,microservices,api

job.experience.min=0
job.experience.max=10

job.max-results=10

job.filter.min-experience=0
job.filter.max-experience=10

# ===================================================
# Email Configuration
# ===================================================

job.email.send-to=perumalraj024@gmail.com
job.email.subject=JobLens - Top 10 Opportunities (Last 24h)
job.email.from=perumalraj@024@gmail.com
job.email.subject-prefix=JobLens -

# ===================================================
# Scheduling Configuration
# ===================================================

scheduling.email-fetcher.cron=0 0 * * * *

# ===================================================
# Logging Configuration
# ===================================================

logging.level.root=INFO
logging.level.com.joblens=DEBUG
logging.level.com.google.api=WARN

logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n

logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

logging.file.name=logs/joblens.log
logging.file.max-size=10MB
logging.file.max-history=10

# ===================================================
# Server Configuration
# ===================================================

server.port=8080
server.servlet.context-path=/
server.shutdown=graceful

spring.lifecycle.timeout-per-shutdown-phase=30s

# ===================================================
# Spring Boot Settings
# ===================================================

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# ===================================================
# Jackson JSON Configuration
# ===================================================

spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.indent-output=true

# ===================================================
# Gmail Fetch Settings
# ===================================================

gmail.max-emails-per-run=20
gmail.lookback-hours=24
gmail.query.subject-terms=jobs,recommended,opportunities

# ===================================================
# Health Checks
# ===================================================

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized